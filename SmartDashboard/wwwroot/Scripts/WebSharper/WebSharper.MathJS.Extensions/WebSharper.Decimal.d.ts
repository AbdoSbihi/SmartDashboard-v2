import { BigNumber } from "mathjs"
export function CreateDecimalBits(bits:number[]):BigNumber
export function CreateDecimal(lo:number, mid:number, hi:number, isNegative:boolean, scale:number):BigNumber
export function WSDecimalMath():MathInstance
